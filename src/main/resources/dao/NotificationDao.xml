<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.NotificationDao">

    <select id="getNotification" resultType="bo.ucb.edu.ingsoft.model.Notification">
        SELECT notification_id as notificationId,
        user_id as userId,
        title as title,
        message as message,
        date as date,
        status as status
        FROM notification
        where user_id=#{userId} and status=1
    </select>
    <select id="detailsByNotificationId" resultType="bo.ucb.edu.ingsoft.model.Notification">
        SELECT notification_id as notificationId,
        user_id as userId,
        title as title,
        message as message,
        date as date,
        status as status
        FROM notification
        where notification_id=#{notificationId} and status=1
    </select>
    <update id="UpdateStatus" parameterType="bo.ucb.edu.ingsoft.model.Notification">
        Update notification
        set
            status=#{status}
        where
            notification_id = #{notificationId}
    </update>
</mapper>