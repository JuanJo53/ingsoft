<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.CertificateDao">
    <select id="detailsByCertificateId" resultType="bo.ucb.edu.ingsoft.model.Certificate">
        SELECT
            certificate_id as certificateId,
            user_id as userId,
            name as name,
            company as company,
            expedition_date as expeditionDate,
            credential_id as credentialId,
            credential_url as credentialURL
            expiration_Date as expirationDate,
        FROM certificate
        WHERE
            certificate_id = #{certificateId}
    </select>
    <select id="getCertificates" resultType="bo.ucb.edu.ingsoft.model.Certificate">
        SELECT
               certificate_id as certificateId,
               name as name,
               company as company,
               expedition_Date as expeditionDate,
               expiration_Date as expirationDate,
        FROM certificate
        WHERE
            user_id = #{userId}
    </select>
    <insert id="newCertificate" parameterType="bo.ucb.edu.ingsoft.model.Certificate">
        INSERT INTO certificate VALUES (
            null, #{userId}, #{name}, #{company},#{expeditionDate},#{credentialId},#{credentialURL},#{expirationDate},1,"192.168.1.1",1, now()
        )
    </insert>

</mapper>
